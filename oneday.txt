
[2026-06-29 수정 사항 요약]
1. 탐색 노드(선택지) 전투 강제 진입 버그 수정
- 원인: 선택지 버튼 재사용 시 Inspector에 바인딩된 기본 전투 진입 이벤트(EnterNode)가 남아 중복 실행됨
- 수정: 버튼 이벤트 할당 전 onClick을 새로 초기화(new Button.ButtonClickedEvent())하여 기존 이벤트 제거

2. 배틀 종료 후 탐색 씬(UI) 복귀 불가 버그 수정
- 원인: 배틀 진입 시 카메라와 UI를 껐는데, 돌아올 때 켜진 오브젝트만 찾는 FindObjectsByType 특성상 꺼진 카메라와 캔버스를 찾지 못함
- 수정: 끌 때 해당 오브젝트들을 hiddenObjects 리스트에 기록해두고, 돌아올 때 리스트를 순회하며 명시적으로 켬

3. 스킬 데이터 빈 값(0) 시 데미지 미적용 및 밸런스 붕괴 문제 해결
- 원인: basePower, coinPower, coinCount 중 하나라도 0이면 전체 스킬을 무시하거나, 그대로 0으로 계산됨
- 수정: 각 능력치가 0 이하일 경우 개별적으로 임시 기본값(basePower=4, coinPower=3, coinCount=1)을 강제 주입하여 0으로 계산되지 않도록 예외 처리 (Player, Enemy 모두 적용)

4. 탐색 노드 크기 조절
- 원인: 노드의 클릭/터치 영역을 넓게 확보할 필요성
- 수정: 노드 생성 시 RectTransform의 sizeDelta를 (100, 100)으로 명시적 지정

2026-06-30: Added dynamic enemy spawning based on Node event type (normal/boss prefabs).
